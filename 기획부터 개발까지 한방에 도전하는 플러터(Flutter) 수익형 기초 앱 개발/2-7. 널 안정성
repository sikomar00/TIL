void main() {
    string name = 'hongdroid';
    string name2 = null; //null (값이 비어있다, 변수는 선언되고 존재하지만 그에 할당된 값은 비어있음 ) > 값을 비워주는 경우가 필요
    string? name2 = null; //변수타입 뒤에 물음표를 붙여주면 널 세이프티가 적용, null 값을 넣어줄수 있게됨(물음표는 곧, name2에 null이 들어와도 된다는 의미(허용함), 다른 값 넣어도 됨 )
    name2 = 'hongdroid';
    print(name2.length);
}
